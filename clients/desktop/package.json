{
    "name": "duskcue-desktop",
    "version": "0.1.0",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "npm --prefix ../web run dev -- --host 127.0.0.1 --port 5173",
        "build": "node ./scripts/build-web-static.mjs",
        "tauri": "tauri",
        "tauri:dev": "tauri dev",
        "tauri:build": "tauri build"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.11.4",
        "vite": "^6.0.0"
    }
}
